%h1 New Review for #{@movie.title}

= form_tag movie_reviews_path(@movie) do

  = render :partial => 'movie_review_form'
  %h5
    = submit_tag 'Create Review',  class: 'btn'
    
    = link_to 'Cancel' , movie_path(@movie),  class: 'btn'
