%label How many potatoes:
-if @moviegoer_reviews
  = select_tag 'reviews[potatoes]', options_for_select((1..5), @moviegoer_reviews.potatoes)
-else
  = select_tag 'reviews[potatoes]', options_for_select(1..5)

%label Comments
= text_area :reviews, :comments
