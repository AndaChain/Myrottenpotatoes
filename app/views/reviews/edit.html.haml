%h1 Update Review for #{@movie.title}

= form_tag movie_review_path(@movie, @reviews), :method => :put do
  
  = render :partial => 'movie_review_form'
  
  %h5
    = submit_tag 'Update Review',  class: 'btn'
    
    = link_to 'Cancel' , movie_review_path(@movie),  class: 'btn'

